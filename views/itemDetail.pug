extends layout 

block content 
  h1= item.name
  div Category:
  ul 
    each category in item.category 
      li 
        a(href=category.url) #{category.name}

  div #{item.summary}
  div Price: #{item.price}
  div Quantity: #{item.stock}
  a(href=item.url + '/update') #{'Update Item Detail'}